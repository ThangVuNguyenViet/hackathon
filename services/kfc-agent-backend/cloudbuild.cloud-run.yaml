steps:
  - name: gcr.io/cloud-builders/docker
    args:
      - build
      - --file
      - services/kfc-agent-backend/Dockerfile.cloud-run
      - --tag
      - ${_IMAGE_URI}
      - .
images:
  - ${_IMAGE_URI}
